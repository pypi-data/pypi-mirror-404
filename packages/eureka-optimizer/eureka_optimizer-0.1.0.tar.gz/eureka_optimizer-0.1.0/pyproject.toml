[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "eureka-optimizer"
version = "0.1.0"
description = "Hybrid ML-guided compiler optimizer"
readme = "README.md"
license = {text = "MIT"}
authors = [{name = "Felipe Alves das Mercês", email = "felipemercessesi@gmail.com"}]
requires-python = ">=3.8"
dependencies = [
    "numpy>=1.20",
    "torch>=1.9",
]

[project.urls]
Homepage = "https://github.com/fmrcs64/eureka-optimizer"