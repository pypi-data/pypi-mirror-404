repos:
  # Standard hooks
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-toml
      - id: check-json
      - id: check-added-large-files
      - id: check-merge-conflict
      - id: debug-statements

  # Python formatting and linting
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.11.13
    hooks:
      - id: ruff
        args: [--fix, --exit-non-zero-on-fix, --config=pyproject.toml]
      - id: ruff-format
        exclude: ^src/mkdocs_mermaid_to_image/_version\.py$

  # Type checking
  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.11.0
    hooks:
      - id: mypy
        additional_dependencies: [
          mkdocs>=1.4.0,
          mkdocs-material>=8.0.0,
          Pillow>=8.0.0,
          numpy>=1.20.0,
          types-Pillow
        ]
        args: [--strict]
        files: ^src/

  # Security
  - repo: https://github.com/PyCQA/bandit
    rev: 1.7.9
    hooks:
      - id: bandit
        args: [-c, pyproject.toml]
        additional_dependencies: ["bandit[toml]"]

  # Dependency vulnerability scanning
  - repo: https://github.com/pypa/pip-audit
    rev: v2.7.3
    hooks:
      - id: pip-audit
        args: [--desc, --fix, --ignore-vuln, GHSA-w853-jp5j-5j7f, --ignore-vuln, GHSA-58pv-8j8x-9vj2, --ignore-vuln, GHSA-597g-3phw-6986, --ignore-vuln, GHSA-983w-rhvv-gwmv, --ignore-vuln, GHSA-qmgc-5h2g-mvrw, --ignore-vuln, GHSA-38jv-5279-wg99]

  # Secret detection (optional - uncomment if needed)
  # - repo: https://github.com/Yelp/detect-secrets
  #   rev: v1.5.0
  #   hooks:
  #     - id: detect-secrets
  #       args: [--baseline, .secrets.baseline]
