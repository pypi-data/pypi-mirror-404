# Activate uv virtual environment automatically
# Requires direnv: https://direnv.net/

# Create venv if it doesn't exist
test -d .venv || uv venv

# Activate the virtual environment
source .venv/bin/activate
