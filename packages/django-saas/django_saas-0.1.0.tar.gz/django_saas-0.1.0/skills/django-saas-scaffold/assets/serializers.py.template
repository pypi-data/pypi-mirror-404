from rest_framework import serializers
from ..models import ${feature_name}

class ${feature_name}Serializer(serializers.ModelSerializer):
    class Meta:
        model = ${feature_name}
        exclude = ['tenant']
        read_only_fields = ['id', 'created_at', 'updated_at']
