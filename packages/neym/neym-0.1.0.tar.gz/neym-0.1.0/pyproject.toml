[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "neym"
version = "0.1.0"
description = "A simple name generator library"
authors = [{ name = "Developed by Arif" }]
readme = "README.md"
license = "MIT"
requires-python = ">=3.8"

[tool.setuptools]
packages = ["core", "generators"]
