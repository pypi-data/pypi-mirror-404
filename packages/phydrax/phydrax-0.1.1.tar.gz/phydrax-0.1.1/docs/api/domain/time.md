# Time

`TimeInterval` is an alias of `ScalarInterval` with default label `"t"`. Use
`ScalarInterval(..., label="...")` for generic scalar axes.

::: phydrax.domain.TimeInterval
    options:
        members:
            - __init__
            - sample
            - fixed
            - extent
            - measure
            - bounds
