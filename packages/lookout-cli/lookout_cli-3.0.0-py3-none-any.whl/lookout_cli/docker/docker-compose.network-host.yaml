name: lookout

services:
  lookout_core:
    network_mode: host

  lookout_ui:
    ports:
      - "4000:4000"

  lookout_greenstream:
    network_mode: host

  lookout_docs:
    ports:
      - "4010:3000"

