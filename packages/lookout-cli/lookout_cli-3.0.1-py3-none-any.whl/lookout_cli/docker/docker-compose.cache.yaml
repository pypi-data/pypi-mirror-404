services:
  lookout_core:
    build:
      cache_from:
        - type=gha
      cache_to:
        - type=gha,mode=max

  lookout_ui:
    build:
      cache_from:
        - type=gha
      cache_to:
        - type=gha,mode=max

  lookout_greenstream:
    build:
      cache_from:
        - type=gha
      cache_to:
        - type=gha,mode=max

  lookout_docs:
    build:
      cache_from:
        - type=gha
      cache_to:
        - type=gha,mode=max

  lookout_proxy:
    build:
      cache_from:
        - type=gha
      cache_to:
        - type=gha,mode=max
