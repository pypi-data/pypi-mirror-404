[project]
name = "anbay"
version = "0.0.1"
description = "Reserved package name"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "Naji"}]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
