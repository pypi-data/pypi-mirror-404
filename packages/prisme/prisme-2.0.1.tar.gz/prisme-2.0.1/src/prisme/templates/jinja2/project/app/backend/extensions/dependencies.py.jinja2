{# app/backend/extensions/dependencies.py — FastAPI dependency overrides #}
"""Custom dependency overrides for {{ project_name }}.

Add FastAPI dependency overrides here. These will be discovered
automatically by the composition root and applied to the app.

Example::

    from fastapi import Depends

    async def get_current_user():
        ...

    dependency_overrides = {
        # original_dependency: override_dependency,
    }
"""

dependency_overrides: dict = {}
