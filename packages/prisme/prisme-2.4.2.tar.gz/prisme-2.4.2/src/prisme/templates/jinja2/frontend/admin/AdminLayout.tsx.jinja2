/**
 * Admin layout wrapper.
 *
 * ⚠️ AUTO-GENERATED BY PRISM - DO NOT EDIT
 */

import { NavLink, Outlet } from 'react-router-dom';

export default function AdminLayout() {
  return (
    <div className="flex h-full">
      <nav className="w-56 border-r border-nordic-200 bg-nordic-50 p-4 space-y-1">
        <h2 className="text-sm font-semibold text-nordic-500 uppercase tracking-wider mb-3">
          Admin
        </h2>
        <NavLink
          to="{{ admin_path }}"
          end
          className={({ isActive }) =>
            `block px-3 py-2 rounded text-sm ${
              isActive
                ? 'bg-nordic-100 text-nordic-900 font-medium'
                : 'text-nordic-600 hover:bg-nordic-50'
            }`
          }
        >
          Dashboard
        </NavLink>
        <NavLink
          to="{{ admin_path }}/users"
          className={({ isActive }) =>
            `block px-3 py-2 rounded text-sm ${
              isActive
                ? 'bg-nordic-100 text-nordic-900 font-medium'
                : 'text-nordic-600 hover:bg-nordic-50'
            }`
          }
        >
          Users
        </NavLink>
        <NavLink
          to="{{ admin_path }}/whitelist"
          className={({ isActive }) =>
            `block px-3 py-2 rounded text-sm ${
              isActive
                ? 'bg-nordic-100 text-nordic-900 font-medium'
                : 'text-nordic-600 hover:bg-nordic-50'
            }`
          }
        >
          Whitelist
        </NavLink>
      </nav>
      <main className="flex-1 p-6 overflow-auto">
        <Outlet />
      </main>
    </div>
  );
}
