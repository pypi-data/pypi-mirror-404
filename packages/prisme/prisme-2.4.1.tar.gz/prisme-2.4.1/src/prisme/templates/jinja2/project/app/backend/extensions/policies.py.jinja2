{# app/backend/extensions/policies.py — auth/authorization policies #}
"""Authorization policies for {{ project_name }}.

Add custom auth policies here. These will be discovered
automatically by the composition root.

Example::

    from fastapi import Request

    async def require_admin(request: Request):
        ...
"""

policies: dict = {}
