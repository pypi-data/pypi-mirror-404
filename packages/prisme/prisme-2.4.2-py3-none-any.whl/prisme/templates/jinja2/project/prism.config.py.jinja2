"""Prism configuration for {{ project_name }}."""

from prisme import PrismConfig

config = PrismConfig(
    spec_path="specs/models.py",
    backend_path="packages/backend/src",
    frontend_path="packages/frontend",
    backend_module_name="{{ project_name_snake }}",
    backend_port=8000,
    frontend_port=5173,
    database_url_env="DATABASE_URL",
    enable_mcp=True,
    enable_graphql=True,
    enable_rest=True,
)
