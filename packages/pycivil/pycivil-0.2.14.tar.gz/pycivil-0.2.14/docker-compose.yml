version: "3.3"

services:
  codeaster:
    image: lpaone/code_aster_whale:latest
    restart: always
    ports:
      - "8100:8100"
    volumes:
      - pycivil:/app/working-data

volumes:
  pycivil:
  pycivilxls:
  pycivil-mongo:
