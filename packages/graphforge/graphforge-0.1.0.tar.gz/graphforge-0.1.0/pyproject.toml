[project]
name = "graphforge"
version = "0.1.0"
description = "Composable graph tooling for analysis, construction, and refinement"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "David Spencer" }]
keywords = ["graph", "opencypher", "pydantic", "analysis"]

dependencies = [
  "pydantic>=2.6",
]

[project.urls]
Homepage = "https://github.com/your-org/graphforge"
Repository = "https://github.com/your-org/graphforge"
Issues = "https://github.com/your-org/graphforge/issues"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = true

[tool.ruff]
line-length = 100
target-version = "py310"
