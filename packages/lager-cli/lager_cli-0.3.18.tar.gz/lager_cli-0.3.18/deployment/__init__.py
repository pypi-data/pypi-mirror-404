"""
Deployment scripts packaged with the CLI.

These scripts are used by `lager install` and related commands to deploy
box code to remote machines. They are included in the package so that
`lager install` works from pip-installed versions, not just from the
lager-mono repository.
"""
