[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    requires_embedding: marks tests as requiring the embedding model (may be slow)
