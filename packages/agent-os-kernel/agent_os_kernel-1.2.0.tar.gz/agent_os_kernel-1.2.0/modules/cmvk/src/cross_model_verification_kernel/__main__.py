"""
Cross-Model Verification Kernel (CMVK)

Allows running the CLI via: python -m cross_model_verification_kernel
"""

from cross_model_verification_kernel.cli import app

if __name__ == "__main__":
    app()
