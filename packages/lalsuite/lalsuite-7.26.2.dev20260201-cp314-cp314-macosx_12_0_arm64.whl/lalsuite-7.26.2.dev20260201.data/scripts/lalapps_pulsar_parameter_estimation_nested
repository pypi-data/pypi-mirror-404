#!python
import os
import sys
from importlib.resources import files, as_file
with as_file(files('lalapps') / 'bin' / 'lalapps_pulsar_parameter_estimation_nested') as path:
    os.execv(path, sys.argv)
