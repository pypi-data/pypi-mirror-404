# Issue 05: ドキュメントアクセス性改善

## 概要

PyPI やインストール後にドキュメントへのアクセスを改善する。

## 採用する改善案

### 案1: TUTORIAL.md にドキュメントリンクを追加

チュートリアル末尾に以下を追加：

```markdown
## 次のステップ

チュートリアルを完了しました！さらに詳しく学ぶには：

- [Railway Framework ドキュメント](https://pypi.org/project/railway-framework/)
- [ソースコード・Issue](https://github.com/aoisakanana/railway_py)
```

> **Note:** GitHub を公開リポジトリにすれば README.md が参照可能。
> PyPI の詳細画面には README.md の内容が表示される。

### 案2: railway docs コマンドの追加

```bash
$ railway docs
Opening https://pypi.org/project/railway-framework/ in browser...

$ railway docs --readme
Opening https://github.com/xxx/railway-framework#readme in browser...
```

実装：
```python
# src/railway/cli/docs.py
import typer
import webbrowser

app = typer.Typer()

PYPI_URL = "https://pypi.org/project/railway-framework/"
GITHUB_URL = "https://github.com/xxx/railway-framework"

@app.command()
def docs(
    readme: bool = typer.Option(False, "--readme", "-r", help="Open GitHub README"),
):
    """Open Railway Framework documentation in browser."""
    url = f"{GITHUB_URL}#readme" if readme else PYPI_URL
    typer.echo(f"Opening {url} ...")
    webbrowser.open(url)
```

### 案3: インラインヘルプの充実

各コマンドのヘルプに詳細説明とドキュメントリンクを追加：

```bash
$ railway new node --help

Usage: railway new node [OPTIONS] NAME

  Create a new node with the given name.

  A node is a pure function decorated with @node that processes data
  in a pipeline. Each node should have a single responsibility.

  Example:
    railway new node fetch_data
    railway new node process_data --no-test

Options:
  --no-test    Skip test file generation
  --help       Show this message and exit

📚 Documentation: https://pypi.org/project/railway-framework/
```

## 不採用の案

### 案4: ドキュメントサイトの整備

**理由:** GitHub を公開リポジトリにすれば、既存の README.md が参照可能。
PyPI の詳細画面にも README.md の内容が表示されるため、別途ドキュメントサイトを構築する必要はない。

## 実装タスク

- [ ] TUTORIAL.md テンプレートに「次のステップ」セクション追加
- [ ] `railway docs` コマンド実装
- [ ] 各 CLI コマンドのヘルプメッセージ拡充
- [ ] PyPI の long_description が README.md を正しく参照しているか確認

## 関連ファイル

- `src/railway/templates/TUTORIAL.md`
- `src/railway/cli/` 各コマンド
- `pyproject.toml`（PyPI メタデータ）
- `README.md`

## 優先度

Low-Medium - 機能には影響しないが、フレームワーク採用の障壁を下げる
