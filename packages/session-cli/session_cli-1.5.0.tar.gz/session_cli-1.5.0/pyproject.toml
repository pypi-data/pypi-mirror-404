[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "session-cli"
version = "1.5.0"
description = "Python CLI tool and library for programmatic control of Session Desktop"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [
    {name = "Aman Verasia", email = "verasiaaman3@gmail.com"}
]
maintainers = [
    {name = "Aman Verasia", email = "verasiaaman3@gmail.com"}
]
keywords = ["session", "messenger", "desktop", "cli", "cdp", "sqlcipher", "privacy", "encryption"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Intended Audience :: End Users/Desktop",
    "Topic :: Communications :: Chat",
    "Topic :: Security :: Cryptography",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Operating System :: MacOS",
    "Operating System :: POSIX :: Linux",
    "Environment :: Console",
    "Typing :: Typed",
]
dependencies = [
    "sqlcipher3>=0.5.0",
    "pynacl>=1.5.0",
    "websocket-client>=1.0.0",
    "pyaes>=1.6.0",
    "pyyaml>=6.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "black>=23.0.0",
    "flake8>=6.0.0",
    "mypy>=1.0.0",
    "pre-commit>=3.6.0",
]

[project.scripts]
session-cli = "session_controller.cli:main"

[project.urls]
Homepage = "https://github.com/amanverasia/session-cli"
Documentation = "https://github.com/amanverasia/session-cli#readme"
Repository = "https://github.com/amanverasia/session-cli"
"Bug Reports" = "https://github.com/amanverasia/session-cli/issues"
"Source" = "https://github.com/amanverasia/session-cli"

[tool.setuptools]
packages = ["session_controller"]
package-data = {"session_controller" = ["py.typed"]}

[tool.black]
line-length = 100
target-version = ['py310', 'py311', 'py312']
include = '\.pyi?$'
extend-exclude = '''
/(
  # directories
  \.eggs
  | \.git
  | \.hg
  | \.mypy_cache
  | \.tox
  | \.venv
  | build
  | dist
)/
'''

[tool.isort]
profile = "black"
line_length = 100

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = false
