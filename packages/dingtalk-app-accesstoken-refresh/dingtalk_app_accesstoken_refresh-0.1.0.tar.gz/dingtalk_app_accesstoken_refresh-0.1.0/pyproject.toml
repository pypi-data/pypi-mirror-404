[project]
name = "dingtalk-app-accesstoken-refresh"
version = "0.1.0"
description = "DingTalk App AccessToken Refresh Tool"
readme = "README.md"
authors = [{ name = "Martian Bugs" }]
requires-python = ">=3.12"
dependencies = ["requests>=2.32.5,<3.0.0", "tinydb>=4.8.2,<5.0.0"]

[build-system]
requires = ["uv_build>=0.9.18,<0.10.0"]
build-backend = "uv_build"
