$ sudo pacman -S ${MINGW_PACKAGE_PREFIX}-boost ${MINGW_PACKAGE_PREFIX}-bzip2 \
      ${MINGW_PACKAGE_PREFIX}-cddlib ${MINGW_PACKAGE_PREFIX}-cliquer \
      ${MINGW_PACKAGE_PREFIX}-cmake ${MINGW_PACKAGE_PREFIX}-curl \
      ${MINGW_PACKAGE_PREFIX}-flint ${MINGW_PACKAGE_PREFIX}-freetype \
      ${MINGW_PACKAGE_PREFIX}-gc ${MINGW_PACKAGE_PREFIX}-gengetopt \
      ${MINGW_PACKAGE_PREFIX}-gettext ${MINGW_PACKAGE_PREFIX}-gf2x \
      ${MINGW_PACKAGE_PREFIX}-glpk ${MINGW_PACKAGE_PREFIX}-gmp \
      ${MINGW_PACKAGE_PREFIX}-gsl ${MINGW_PACKAGE_PREFIX}-iml \
      ${MINGW_PACKAGE_PREFIX}-libatomic_ops \
      ${MINGW_PACKAGE_PREFIX}-libbraiding ${MINGW_PACKAGE_PREFIX}-libffi \
      ${MINGW_PACKAGE_PREFIX}-libgd ${MINGW_PACKAGE_PREFIX}-libhomfly \
      ${MINGW_PACKAGE_PREFIX}-libpng ${MINGW_PACKAGE_PREFIX}-lrcalc \
      ${MINGW_PACKAGE_PREFIX}-m4ri ${MINGW_PACKAGE_PREFIX}-m4rie \
      ${MINGW_PACKAGE_PREFIX}-meson ${MINGW_PACKAGE_PREFIX}-mpc \
      ${MINGW_PACKAGE_PREFIX}-mpfi ${MINGW_PACKAGE_PREFIX}-mpfr \
      ${MINGW_PACKAGE_PREFIX}-nauty ${MINGW_PACKAGE_PREFIX}-ncurses \
      ${MINGW_PACKAGE_PREFIX}-ninja ${MINGW_PACKAGE_PREFIX}-ntl \
      ${MINGW_PACKAGE_PREFIX}-openblas ${MINGW_PACKAGE_PREFIX}-openssl \
      ${MINGW_PACKAGE_PREFIX}-planarity ${MINGW_PACKAGE_PREFIX}-ppl \
      ${MINGW_PACKAGE_PREFIX}-python ${MINGW_PACKAGE_PREFIX}-python-appdirs \
      ${MINGW_PACKAGE_PREFIX}-python-cachetools \
      ${MINGW_PACKAGE_PREFIX}-python-chardet \
      ${MINGW_PACKAGE_PREFIX}-python-colorama \
      ${MINGW_PACKAGE_PREFIX}-python-distlib \
      ${MINGW_PACKAGE_PREFIX}-python-filelock \
      ${MINGW_PACKAGE_PREFIX}-python-tox \
      ${MINGW_PACKAGE_PREFIX}-python-virtualenv ${MINGW_PACKAGE_PREFIX}-qhull \
      ${MINGW_PACKAGE_PREFIX}-rankwidth ${MINGW_PACKAGE_PREFIX}-readline \
      ${MINGW_PACKAGE_PREFIX}-suitesparse ${MINGW_PACKAGE_PREFIX}-symmetrica \
      ${MINGW_PACKAGE_PREFIX}-texinfo ${MINGW_PACKAGE_PREFIX}-xz \
      ${MINGW_PACKAGE_PREFIX}-zeromq ${MINGW_PACKAGE_PREFIX}-zlib bc binutils \
      flex gcc m4 make perl python tar which
