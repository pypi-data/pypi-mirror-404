# https://python-poetry.org/docs/pyproject

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.poetry]
name = "logger-Local"
version = "0.0.7" # https://pypi.org/project/logger-local-python-package
description = "Logger Local Python Package"
readme = "README.md"
authors = [
    "Circlez.ai <info@circlez.ai>",
]
