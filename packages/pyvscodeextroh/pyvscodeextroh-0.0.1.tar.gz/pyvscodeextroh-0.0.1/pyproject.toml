[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pyvscodeextroh"
version = "0.0.1"
description = "A python package to manage vscode extensions."
readme = "README.md"
requires-python = ">=3.7"
license = {file = "LICENSE"}
authors = [
  {name = "IT-Administrators" },
]
maintainers = [
  {name = "IT-Administrators"}
]
keywords = ["extensions","extension","vscode","manage"]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: POSIX :: Linux",
]

[project.urls]
"Homepage" = "https://github.com/IT-Administrators/pyvscodeextroh"