[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "LastFrame"
version = "1.0.0"
description = "A logging example application with sudo detection and log rotation"
readme = "README.md"
requires-python = ">=2.7, !=3.0.*, !=3.1.*, !=3.2.*, !=3.3.*, !=3.4.*"
license = {text = "MIT"}
authors = [
    {name = "Wilgat Wong", email = "wilgat.wong@gmail.com"}
]
keywords = ["logging", "sudo", "chronicle", "example"]

dependencies = [
    "ChronicleLogger>=1.2.3",
    "opencv-python"
]

# Official project links (this is what shows on PyPI!)
[project.urls]
Homepage = "https://github.com/Wilgat/LastFrame"
Repository = "https://github.com/Wilgat/LastFrame"
Issues = "https://github.com/Wilgat/LastFrame/issues"

# This makes `last-frame` command available after pip install
[project.scripts]
last-frame = "LastFrame.cli:main"