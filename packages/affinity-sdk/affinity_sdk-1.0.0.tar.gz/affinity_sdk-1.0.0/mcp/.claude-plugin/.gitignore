# Extracted MCP server (created on first run)
.mcp-extracted/

# Auto-generated registry cache (mcp-bash-framework)
.registry/

# Note: These files ARE tracked (committed to git):
# - xaffinity-mcp.sh (wrapper script - NOT the same as mcp/xaffinity-mcp.sh)
# - plugin.json
# - skills/
#
# These files ARE tracked (built and committed by CI):
# - xaffinity-mcp-plugin.zip (the MCP server bundle)
# - VERSION (for version-based re-extraction)
