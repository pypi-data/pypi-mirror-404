{
  "name": "execute-write-command",
  "description": "Execute a write CLI command (create, update, delete, add, remove). Use discover-commands with category='write' first.\n\nNote: Do not include --json in argv; JSON output is automatic.\n\nCommon operations:\n- Create entities: person create, company create, opportunity create\n- Add to lists: entry add \"<list>\" --person-id <id> (or --company-id)\n- Log interactions: interaction create --type meeting --person-id <id>\n- Update list entry fields: entry field \"<list>\" <entryId> --set <field> <value>\n\nExamples:\n- person create --first-name John --last-name Doe --emails john@example.com\n- interaction create --type meeting --direction outgoing --person-id 123\n- note create --person-id 456 --content 'Meeting notes here'\n- entry field \"Dealflow\" 123 --set Status \"Active\"\n\nDestructive commands (delete, remove) require confirm=true.",
  "inputSchema": {
    "type": "object",
    "required": ["command"],
    "properties": {
      "command": {
        "type": "string",
        "description": "CLI command path (e.g., 'person create', 'list-entry add', 'interaction create'). Use discover-commands with category='write' to find available commands."
      },
      "argv": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Command arguments as array. Use '--flag', 'value' pairs. Example: ['--first-name', 'John', '--last-name', 'Doe', '--emails', 'john@example.com']"
      },
      "confirm": {
        "type": "boolean",
        "default": false,
        "description": "REQUIRED for destructive commands (delete, remove). Set to true to confirm the operation. Automatically appends --yes to CLI."
      },
      "dryRun": {
        "type": "boolean",
        "default": false,
        "description": "If true, return the command that would be executed without running it. Use to verify command before execution."
      }
    }
  },
  "timeoutSecs": 30,
  "timeoutHint": "For batch creates, reduce batch size. Check network connectivity if retries exhaust.",
  "annotations": {
    "readOnlyHint": false,
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": true
  }
}
