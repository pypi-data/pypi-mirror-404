apiVersion: skaffold/v4beta12
kind: Config
metadata:
  name: dpps
profiles:
  - name: light
    build:
      #  artifacts:
      #    - image: harbor.cta-observatory.org/dpps/dpps-clients
      #      docker:
      #        dockerfile: Dockerfile
    deploy:
      helm:
        releases:
          - name: dpps
            chartPath: chart
            valuesFiles:
              - chart/values.yaml
            setValues:
              dev.sleep: true
              dev.run_tests: false
              dev.client_image_tag: "v0.1.0"
              wms.enabled: false
                #     setValueTemplates:
                #          dev.client_image_tag: "{{.IMAGE_TAG }}"
            #version: 0.1.0
            #        hooks:
            #          before:
            #            - host:
            #                command: ["make", "pull-load-helm-images", "N_IMAGE_PULL_JOBS=4", "CHART_EXTRA_VALUES='--set wms.enabled=false'"]
            #                os: [darwin, linux]
