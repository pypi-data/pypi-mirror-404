{# Table section template - simple version #}
{% for col in section.columns %}[bold]{{ col }}[/]{% if not loop.last %} │ {% endif %}{% endfor %}

{{ "─" * 80 }}
{% for row in section.data %}
{% for col in section.columns %}{% set key = col|lower|replace(' ', '_')|replace('(', '')|replace(')', '') %}{{ row.get(key, row.get(col|lower, '')) }}{% if not loop.last %} │ {% endif %}{% endfor %}

{% endfor %}
