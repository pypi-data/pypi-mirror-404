{# Blockers section template #}
{% if section.data -%}
{% for blocker in section.data -%}
{%- set color = {'P0': 'red', 'P1': 'yellow', 'P2': 'blue', 'P3': 'dim'}.get(blocker.priority, 'white') -%}
[{{ color }}]{{ blocker.priority }}[/] [bold]{{ blocker.id }}[/] {{ blocker.title }}
{% for dep in blocker.blocked_by -%}
    ← blocked by [bold]{{ dep.id }}[/] {{ dep.title }} [dim][{{ dep.status }}][/]
{% endfor %}
{% endfor -%}
{% else -%}
[green]No blocked specs![/]
{% endif %}
