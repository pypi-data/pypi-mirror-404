{# Matrix section template #}
[bold]FR Status[/] × [bold]IMPL Status[/] → [bold]Count[/]
{{ "─" * 50 }}
{% for row in section.data -%}
{{ row.fr_status }} × {{ row.impl_status }} → [bold]{{ row.count }}[/]
{% if row.specs -%}
    [dim]Examples: {{ row.specs[:3]|join(', ') }}[/]
{% endif -%}
{% endfor %}
