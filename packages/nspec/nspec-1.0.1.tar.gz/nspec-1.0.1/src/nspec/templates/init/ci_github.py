"""Template for GitHub Actions CI workflow."""

TEMPLATE = """\
# .github/workflows/nspec.yml — Generated by nspec init
name: nspec

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install nspec
        run: pip install nspec

      - name: Validate specifications
        run: nspec --validate

      - name: Generate NSPEC.md
        run: nspec --generate
"""


def render() -> str:
    """Return GitHub Actions workflow content."""
    return TEMPLATE
