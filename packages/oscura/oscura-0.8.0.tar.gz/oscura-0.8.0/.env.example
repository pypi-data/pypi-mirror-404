# Oscura Environment Variables
# Copy this file to .env and fill in your values
# NOTE: .env is in .gitignore and will never be committed

# =============================================================================
# PyPI Publishing (for maintainers only)
# =============================================================================
# Get tokens from:
# - TestPyPI: https://test.pypi.org/manage/account/token/
# - PyPI: https://pypi.org/manage/account/token/

# Test PyPI token (for testing releases)
TESTPYPI_TOKEN=your_test_pypi_token_here

# Production PyPI token (for actual releases)
PYPI_TOKEN=your_pypi_token_here

# =============================================================================
# GitHub API (for CI/CD and automation)
# =============================================================================
# Create at: https://github.com/settings/tokens
# Scopes needed: repo, workflow, write:packages

GITHUB_TOKEN=your_github_token_here

# =============================================================================
# CI/CD Configuration
# =============================================================================
# pytest worker count (auto-detect by default)
PYTEST_WORKERS=auto

# Coverage threshold percentage (fail if below this)
COVERAGE_THRESHOLD=80

# Test timeout in seconds
PYTEST_TIMEOUT=300

# =============================================================================
# Development Environment
# =============================================================================
# Python version to use (3.12 or 3.13)
PYTHON_VERSION=3.12

# uv cache directory (for faster dependency installs)
UV_CACHE_DIR=/tmp/.uv-cache

# Enable strict mode for development
PYTHONWARNINGS=default

# Development mode (shows more verbose output)
DEBUG=false

# =============================================================================
# Documentation
# =============================================================================
# MkDocs strict mode (fail on warnings)
MKDOCS_STRICT=true

# Documentation serve port
MKDOCS_PORT=8000

# =============================================================================
# Security Scanning (optional overrides)
# =============================================================================
# Skip security scans (not recommended)
SKIP_SECURITY_SCAN=false

# Safety API key (optional, for enhanced vulnerability data)
SAFETY_API_KEY=

# =============================================================================
# Performance Testing
# =============================================================================
# Benchmark baseline file
BENCHMARK_BASELINE=tests/performance/baseline_results.json

# Benchmark regression threshold percentage
BENCHMARK_THRESHOLD=15

# =============================================================================
# Claude Code Configuration (optional)
# =============================================================================
# Claude hooks bypass (emergency use only)
CLAUDE_BYPASS_HOOKS=0

# =============================================================================
# Notes
# =============================================================================
# - Variables prefixed with CI_ are set automatically in GitHub Actions
# - Most variables have sensible defaults and don't need to be set
# - Only set variables you need to override from defaults
# - Never commit .env file - it contains secrets!
