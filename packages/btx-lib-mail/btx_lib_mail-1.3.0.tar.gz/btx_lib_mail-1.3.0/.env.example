# Copy to .env and fill values as needed.

# Codecov upload token (required for private repos; optional for public repos)
CODECOV_TOKEN=

# PyPI API token for release workflow (.github/workflows/release.yml)
# Format: pypi-AgENdGVzdC5weXBpLm9yZwIk...
PYPI_API_TOKEN=

# Optional: GitHub token for API-limited tasks
GITHUB_TOKEN=

######################################################################################
### Optional SMTP integration knobs for tests (leave blank to disable live sends)
######################################################################################

# comma-separated hostnames or `host:port` entries tried
TEST_SMTP_HOSTS=

# comma-separated email addresses that should receive the smoke message.
TEST_RECIPIENTS=

# optional envelope sender; defaults to the first recipient when unset.
TEST_SENDER=

# optional boolean toggle (`1`, `true`, `yes`, `on`) enabling STARTTLS before authentication, default = ENABLED
TEST_SMTP_USE_STARTTLS=

# optional credentials used when both values are supplied.
TEST_SMTP_USERNAME=
TEST_SMTP_PASSWORD=

######################################################################################
### CLI ``send`` command defaults.
### only .env files in the current working directory are considered
### those values can be also passed as environment variables and ONLY apply to CLI-Usage
######################################################################################

# comma-separated hostnames or `host:port` entries tried
BTX_MAIL_SMTP_HOSTS=

# comma-separated email addresses that should receive the smoke message.
BTX_MAIL_RECIPIENTS=

# optional envelope sender; defaults to the first recipient when unset.
BTX_MAIL_SENDER=

# optional boolean toggle (`1`, `true`, `yes`, `on`) enabling STARTTLS before authentication, default = ENABLED
BTX_MAIL_SMTP_USE_STARTTLS=

# optional credentials used when both values are supplied.
BTX_MAIL_SMTP_USERNAME=
BTX_MAIL_SMTP_PASSWORD=

# optional socket timeout in seconds, default = 30
BTX_MAIL_SMTP_TIMEOUT=

######################################################################################
### Attachment Security Settings
### Configure attachment validation for the CLI ``send`` command
######################################################################################

# Comma-separated allowed extensions (whitelist mode, e.g., .pdf,.txt,.docx)
# When set, only these extensions are allowed and the blocked list is ignored.
# Leave empty to use blacklist mode (blocked extensions apply).
BTX_MAIL_ATTACHMENT_ALLOWED_EXT=

# Comma-separated blocked extensions (blacklist mode, e.g., .exe,.bat,.sh)
# Only applies when allowed extensions is not set. Overrides OS-specific defaults.
BTX_MAIL_ATTACHMENT_BLOCKED_EXT=

# Comma-separated allowed directories (whitelist mode)
# When set, attachments must be under one of these directories.
BTX_MAIL_ATTACHMENT_ALLOWED_DIRS=

# Comma-separated blocked directories (blacklist mode)
# Attachments from these directories are rejected. Overrides OS-specific defaults.
BTX_MAIL_ATTACHMENT_BLOCKED_DIRS=

# Maximum attachment size in bytes, default = 26214400 (25 MiB)
# Set to empty to disable size checking.
BTX_MAIL_ATTACHMENT_MAX_SIZE=

# Allow symlinked attachments, default = false
# Boolean toggle (`1`, `true`, `yes`, `on` to enable)
BTX_MAIL_ATTACHMENT_ALLOW_SYMLINKS=

# Raise on security violations, default = true
# When true, security violations raise an error. When false, log warning and skip.
# Boolean toggle (`1`, `true`, `yes`, `on` to raise; `0`, `false`, `no`, `off` to warn)
BTX_MAIL_ATTACHMENT_RAISE_ON_SECURITY=
