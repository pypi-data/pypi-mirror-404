version: 1

# Simple team dimension for Grafana filtering.
#
# `members` should list canonical identities (preferred) or aliases.
# Canonicalization is applied via the identity resolver first.
teams: []

