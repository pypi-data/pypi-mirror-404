[project]
name = "contrastive-rl-pytorch"
version = "0.0.29"
description = "Contrastive RL"
authors = [
    { name = "Phil Wang", email = "lucidrains@gmail.com" }
]
readme = "README.md"
requires-python = ">= 3.9"
license = { file = "LICENSE" }
keywords = [
    'artificial intelligence',
    'deep learning',
]

classifiers=[
    'Development Status :: 4 - Beta',
    'Intended Audience :: Developers',
    'Topic :: Scientific/Engineering :: Artificial Intelligence',
    'License :: OSI Approved :: MIT License',
    'Programming Language :: Python :: 3.9',
]

dependencies = [
    "accelerate",
    "einx>=0.3.0",
    "einops>=0.8.1",
    "memmap-replay-buffer>=0.0.17",
    "torch>=2.4",
    "x-mlps-pytorch>=0.2.0"
]

[project.urls]
Homepage = "https://pypi.org/project/contrastive-rl/"
Repository = "https://github.com/lucidrains/contrastive-rl"

[project.optional-dependencies]
examples = []
test = [
    "pytest"
]

[tool.pytest.ini_options]
pythonpath = [
  "."
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.rye]
managed = true
dev-dependencies = []

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["contrastive_rl"]
