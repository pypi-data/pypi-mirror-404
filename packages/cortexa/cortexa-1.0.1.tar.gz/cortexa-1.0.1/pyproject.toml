[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "cortexa"
version = "1.0.1"  
description = "A basic AI model embedded in python"
readme = "readme.md"
authors = [{ name = "Samarth Chugh" }]
license = { text = "MIT" }
requires-python = ">=3.6"
