"""Pass-level tests for deterministic semantic analysis.

These tests are intentionally "contract style": they assert only a few high-signal
fields per pass, instead of snapshotting full payloads (goldens cover that).
"""

