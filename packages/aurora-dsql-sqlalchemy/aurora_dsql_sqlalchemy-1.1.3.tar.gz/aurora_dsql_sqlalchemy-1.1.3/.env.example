CLUSTER_ENDPOINT=your-cluster.dsql.us-east-1.on.aws
CLUSTER_USER=admin

# use 'psycopg' for psycopg3 and 'psycopg2' for psycopg2
DRIVER=psycopg

# path to Amazon root cert if it is not already trusted by the system,
# download from https://www.amazontrust.com/repository/AmazonRootCA1.pem
# SSL_CERT_FILE=/path/to/root.pem
