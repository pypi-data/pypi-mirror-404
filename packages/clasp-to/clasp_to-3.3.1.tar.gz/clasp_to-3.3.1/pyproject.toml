[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "clasp-to"
version = "3.3.1"
description = "CLASP client for Python - Creative Low-Latency Application Streaming Protocol"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.8"
dependencies = [
    "websockets>=10.0",
    "msgpack>=1.0.0",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Multimedia",
    "Topic :: System :: Networking",
]
keywords = ["clasp", "osc", "midi", "dmx", "artnet", "creative", "protocol"]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=0.21",
    "mypy>=1.0",
]

[project.urls]
Homepage = "https://clasp.to"
Documentation = "https://clasp.to/docs/python"
Repository = "https://github.com/lumencanvas/clasp"

[tool.hatch.build.targets.wheel]
packages = ["python/clasp"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.mypy]
python_version = "3.8"
warn_return_any = true
warn_unused_configs = true
