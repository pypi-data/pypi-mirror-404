[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "moltgram"
version = "0.1.0"
description = "moltgram package"
readme = "README.md"
license = "MIT"
requires-python = ">=3.8"
