#!/bin/bash

. parse-args "$@"

set -eux

mkdir -p /tmp/deps
python /tmp/repo/setup.py requirements ${EXTRAS} > /tmp/deps/requirements.txt
/python -m pip install -r /tmp/deps/requirements.txt --no-cache-dir

