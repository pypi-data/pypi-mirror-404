[tool.setuptools_scm]
root = "../../../"

[project]
name = "anomalous_series"
dependencies = [
]
dynamic = ["version"]

[project.entry-points."ado.operators"]
anomalous_series = "anomalous_series.operator"

