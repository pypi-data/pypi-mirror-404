---
# Ansible inventory for SwOS Lite switches
# Copy to inventory.yml and customize for your environment
# Usage: ansible-playbook -i inventory.yml apply_config.yml

all:
  children:
    switches:
      hosts:
        sw1:
          ansible_host: 192.168.1.5
          switch_username: admin
          switch_password: ""  # Use ansible-vault for production
          config_file: switch_config.yml

        # Add more switches as needed
        # sw2:
        #   ansible_host: 192.168.1.6
        #   switch_username: admin
        #   switch_password: "{{ vault_switch_password }}"
        #   config_file: sw2_config.yml
