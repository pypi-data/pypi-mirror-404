[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "wandb-widget"
version = "0.1.0a0"
description = "POC package (beacon-only)"
authors = [{name = "Your Name"}]
readme = "README.md"
requires-python = ">=3.6"

[tool.setuptools]
packages = ["wandb-widget"]
