[build-wandb-widgettem]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "wandb-widget"
version = "0.0.0"
description = "Dummy wandb-widget package for PyPI"
authors = [{name="Your Name", email="you@example.com"}]
readme = "README.md"
requires-python = ">=3.7"

[tool.setuptools.packages.find]
where = ["src"]
