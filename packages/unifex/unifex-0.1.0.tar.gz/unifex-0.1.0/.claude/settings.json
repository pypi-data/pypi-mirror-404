{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "command": "echo '⚠️  TDD: Have you written tests FIRST? Red → Green → Refactor'"
      },
      {
        "matcher": "Edit|Write",
        "command": "echo '⚠️  Check if the new option should be passed to cli to"
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Task",
        "command": "echo '⚠️  Remember: after the feature implementation look into README.md and whole docs/ if changed things need to be updated!'"
      },
      {
        "matcher": "Task",
        "command": "echo '⚠️  Remember: COMMIT when the feature is complete!'"
      },
      {
        "matcher": "Bash",
        "command": "echo '📋 If tests fail due to missing credentials: ask user for creds to make cassettes, or exclude from coverage. Do NOT mock to fake coverage.'"
      }
    ]
  }
}
