# Dev environment configuration for Build Cub
# These settings are loaded when env="dev" or BUILD_CUB_ENV=dev

debug = true
verbose = true
print_time_to_build = true
fail_fast = false  # Try all backends even if one fails during development