# Test environment configuration for Build Cub
# These settings are loaded when env="test" or BUILD_CUB_ENV=test

debug = false
verbose = false
print_time_to_build = true
fail_fast = false  # Try all backends even if one fails during testing
