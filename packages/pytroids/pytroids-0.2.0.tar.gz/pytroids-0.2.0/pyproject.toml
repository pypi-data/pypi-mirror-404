[tool.poetry]
name = "pytroids"
version = "0.2.0"
description = "Python on steroids!"
authors = ["BJS <coderbjs@gmail.com>"]
readme = "README.md"
license = "MIT"
packages = [{ include = "pytroids" }]

[tool.poetry.dependencies]
python = "^3.9"

[tool.poetry.group.dev.dependencies]
pytest = "^7.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
