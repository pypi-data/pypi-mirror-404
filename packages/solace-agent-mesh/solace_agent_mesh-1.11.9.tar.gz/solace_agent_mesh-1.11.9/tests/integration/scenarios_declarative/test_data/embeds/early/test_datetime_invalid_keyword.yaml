test_case_id: "embed_datetime_invalid_keyword_001"
description: "Tests «datetime:invalid_keyword»."
tags: ["all", "common", "embeds"]
skip_intermediate_events: true

gateway_input:
  target_agent_name: "TestAgent"
  user_identity: "embed_tester@example.com"
  parts: [{type: "text", text: "Datetime with invalid keyword"}]
llm_interactions:
  - static_response:
      choices: [message: {role: "assistant", content: "Result: «datetime:invalid_keyword»"}]
expected_gateway_output:
  - type: "final_response"
    kind: task
    id: '*'
    contextId: session_embed_datetime_invalid_keyword_001
    status:
      state: completed
      message:
        kind: message
        messageId: '*'
        role: agent
        parts:
          - type: "text"
            text_contains:
              - "Result: invalid_keyword"
