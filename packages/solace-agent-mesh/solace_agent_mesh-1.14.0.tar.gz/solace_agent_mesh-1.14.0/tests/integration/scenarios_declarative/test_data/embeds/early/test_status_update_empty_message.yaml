test_case_id: "embed_status_update_empty_message_001"
description: "Tests «status_update:» with an empty message. Expects the empty status update to be ignored."
tags: ["all", "common", "embeds"]
skip_intermediate_events: true

gateway_input:
  target_agent_name: "TestAgent"
  user_identity: "embed_tester@example.com"
  parts: [{type: "text", text: "Status update with empty message."}]
llm_interactions:
  - static_response:
      choices: [message: {role: "assistant", content: "«status_update:»This is the main text."}]
expected_gateway_output:
  - type: "status_update"
    kind: task
    id: '*'
    contextId: session_embed_status_update_empty_message_001
    status:
      state: unknown
      message:
        kind: message
        messageId: '*'
        role: agent
    event_purpose: "llm_invocation"
    final_flag: false
  - type: "status_update"
    kind: task
    id: '*'
    contextId: session_embed_status_update_empty_message_001
    status:
      state: unknown
      message:
        kind: message
        messageId: '*'
        role: agent
    event_purpose: "llm_response"
    final_flag: false
  - type: "final_response"
    kind: task
    id: '*'
    contextId: session_embed_status_update_empty_message_001
    status:
      state: "completed"
      message:
        kind: message
        messageId: '*'
        role: agent
        parts:
          - type: "text"
            text_contains:
              - "This is the main text."
