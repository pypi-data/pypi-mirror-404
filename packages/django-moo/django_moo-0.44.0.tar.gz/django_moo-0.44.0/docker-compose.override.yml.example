# This is a simple example override file that mounts the current directory as an overlay.
services:
  nginx:
    volumes:
      - .:/usr/src/app
  webapp:
    volumes:
      - .:/usr/src/app
  shell:
    volumes:
      - .:/usr/src/app
  celery:
    volumes:
      - .:/usr/src/app
