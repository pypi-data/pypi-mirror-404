#!/usr/bin/env python3
"""
Standalone test for MQTT Hub/Spoke without EasyCoder.
Run this to verify the MQTT implementation works correctly.
"""

import sys
sys.path.insert(0, '/home/graham/dev/easycoder/easycoder-py')

# Import the module directly to run the test
from easycoder import ec_mqtt

# The test code will run from the __main__ block in ec_mqtt.py
