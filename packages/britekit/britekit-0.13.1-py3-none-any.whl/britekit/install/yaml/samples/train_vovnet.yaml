# Sample for training scaled VovNet, with recommended hyperparameters.
train:
  train_pickle: "data/training.pkl"
  model_type: "vovnet.1"

  learning_rate: .0015
  drop_rate: 0.0
  drop_path_rate: 0.0

  num_epochs: 10
  save_last_n: 3
  num_workers: 8
