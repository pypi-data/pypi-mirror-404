[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "updogfx"
version = "0.2.7"
description = "A simple file server with Cloudflare tunneling"
readme = "README.md"
authors = [{ name = "EFXTv" }]
dependencies = []

[project.scripts]
updogfx = "updogfx.main:run"
