[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"


[project]
name = "mu-pygrader"
version = "1.0.8"
description = "Grader tools for ENGR 212 at Messiah"
authors = [
    { name = "Ben Weaver", email = "bweaver2@messiah.edu" }
]
dependencies = [
    "numpy",
    "pandas",
    "pyinstaller"
]
