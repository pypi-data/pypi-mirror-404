[build-system]
requires = ["setuptools>=80.9.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "container-manager-mcp"
version = "1.2.11"
description = "Container Manager - manage Docker, Docker Swarm, and Podman containers. MCP+A2A Servers Out of the Box!"
readme = "README.md"
authors = [{ name = "Audel Rouhi", email = "knucklessg1@gmail.com" }]
license = { text = "MIT" }
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "License :: Public Domain",
    "Environment :: Console",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
]
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28.1",
    "fastmcp>=3.0.0b1",
    "eunomia-mcp>=0.3.10",
    "podman>=5.6.0",
]

[project.optional-dependencies]
podman = [
    "podman>=5.6.0",
]
docker = [
    "docker>=7.1.0",
]
a2a = [
    "pydantic-ai-slim[fastmcp,openai,anthropic,google,huggingface,a2a,ag-ui,web]>=1.32.0",
    "pydantic-ai-skills",
    "fastapi>=0.128.0"
]
all = [
    "requests>=2.28.1",
    "fastmcp>=3.0.0b1",
    "eunomia-mcp>=0.3.10",
    "docker>=7.1.0",
    "podman>=5.6.0",
    "pydantic-ai-slim[fastmcp,openai,anthropic,google,huggingface,a2a,ag-ui,web]>=1.32.0",
    "pydantic-ai-skills",
    "fastapi>=0.128.0"
]

[project.scripts]
container-manager = "container_manager_mcp.container_manager:container_manager"
container-manager-mcp = "container_manager_mcp.container_manager_mcp:container_manager_mcp"
container-manager-agent = "container_manager_mcp.container_manager_agent:agent_server"


[tool.setuptools.package-data]
container_manager_mcp = ["mcp_config.json", "skills/**"]

[tool.setuptools.packages.find]
where = ["."]
