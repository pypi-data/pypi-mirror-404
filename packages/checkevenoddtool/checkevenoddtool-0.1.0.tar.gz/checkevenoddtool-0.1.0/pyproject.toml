[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "checkevenoddtool"
version = "0.1.0"
description = "check if input is even or odd"
requires-python = ">=3.8"

[tool.setuptools]
py-modules = ["checkevenoddtool"]
