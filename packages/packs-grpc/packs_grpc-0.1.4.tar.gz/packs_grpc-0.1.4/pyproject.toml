[project]
name = "packs-grpc"
version = "0.1.4"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "grpcio>=1.76.0",
    "grpcio-tools>=1.76.0",
]

[tool.hatch.build]
packages = ["src/packs_grpc"]
