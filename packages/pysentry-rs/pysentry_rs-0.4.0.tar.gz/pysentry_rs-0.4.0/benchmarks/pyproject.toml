[project]
name = "benchmarks"
version = "0.1.0"
description = "Performance benchmark suite for PySentry vs pip-audit"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "matplotlib>=3.10.5",
    "pip-audit>=2.9.0",
    "psutil>=7.0.0",
    "tabulate>=0.9.0",
]
